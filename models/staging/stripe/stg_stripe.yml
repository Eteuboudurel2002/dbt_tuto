version: 2

models:
  - name: stg_payments
    columns:
      - name: payment_id
        tests:
          - unique:
              config:
                severity: warn
          - not_null
          